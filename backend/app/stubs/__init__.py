"""P2 stub interfaces — visibly labelled as stubbed in the UI too
(see ``frontend/components/atoms.tsx::StubBadge``).

Each module below defines the CONTRACT the corresponding P2 integration
must satisfy, so the concrete implementation can drop in with no
changes to callers.
"""
